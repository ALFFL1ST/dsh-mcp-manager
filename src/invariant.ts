/** Invariant companion plugin (no assertions — nothing to check at runtime). */

/** Provides no assertions: the mcp-manager plugin owns no cross-package runtime invariants. */
export function apply(): void {}
